@page
@model IndexModel
@{
    ViewData["Title"] = "Case Classifier";
}
@addTagHelper *, Microsoft.AspNetCore.MVC.TagHelpers
<!--#<form method="post">
    <div classs="form-row">
        <div asp-validation-summary="All"></div>
        <div class="form-group" col-md-6">
            <label asp-for=""/>
            <input asp-for="" class="form-control" />

        </div>
    </div>
</form>-->


<h1>Auto Case Classifier</h1>

<h3>Start to type a message to find prediction of Model</h3>

<form method="post">
    <label><b>Message :</b></label><textarea asp-for="@Model.CategoryPrediction.SentimentText" type="text" id="txt1" onkeyup="showHint(this.value)">"@Model.SentimentData"</textarea>
    <button>Result:</button>
</form>

<p>Case Category: <span asp-for="@Model.CategoryPrediction.Result" id="txtHint">@Model.CategoryPrediction.Result</span></p>


<!-- <script>
function showHint(str) {
  var xhttp;
  if (str.length == 0) {
    document.getElementById("txtHint").innerHTML = "";
    return;
  }
  xhttp = new XMLHttpRequest();
  xhttp.onreadystatechange = function() {
    if (this.readyState == 4 && this.status == 200) {
      document.getElementById("txtHint").innerHTML = this.responseText;
    }
  };
  xhttp.open("GET", prediction(str), true);
  xhttp.send();
}
    </script>-->
